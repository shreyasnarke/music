touch .gitignore
git config --global user.name "shreyasnarke"
 git config --global user.email "narkeshreyas44@gmail.com"
git init #intialize repo
git .add #all files in staging area to put files in github
git commit -m "Initial Commit" #to save files data
git status #only for just see working is clear or not
##to genarate ssh file
 ssh-keygen -t rsa -b 4096 -C "narkeshreyas44@gmail.com"
##tail/after genrating <path id_rsa.pub> #add this key to your github account in new ssh key with name

git remote add origin <ssh url.git>  #url present in github your spec repo code in ssh 
git push origin master

<<
git status
git add .
git commit -m "Added cheatsheet"
git push origin master
>>